/**
 * Type declarations for react-native-voice (no upstream @types package).
 * Declares the module as 'any' typed so imports compile without errors.
 */

declare module 'react-native-voice' {
  const Voice: any;
  export default Voice;
}